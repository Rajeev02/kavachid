apiVersion: v1
kind: Service
metadata:
  name: {{ include "kavachid.fullname" . }}
  labels:
    {{- include "kavachid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kavachid.selectorLabels" . | nindent 4 }}
